# Copy this file to ".env" in the project root and fill in your key.
# Required only if you want Gemini-based AI evaluation
# (Interview/ai_evaluator.py). The app works without it using the
# local rule-based evaluator (Interview/evaluator.py).
GEMINI_API_KEY=your-gemini-api-key-here
